# 梨园铁律

2026-08-10 立。连环事故（格式块丢失 → 围栏启发式补丁 → 提示词把模型搅得把状态栏写进稿件）暴露的不是某个 bug，而是开发方式本身：**用各种情况的堆叠去处理问题，完全扼杀了 agent 的灵活性**。每一层堆叠都在替模型思考，把灵活性换成服从，并对没见过的卡构成污染。以下铁律高于任何单次修复的诉求，对本仓库的一切改动生效。

## 一、禁止堆叠提示词

出了问题不许靠「再加一句话」解决：不加催告、不加点名、不加提醒，不往 system prompt / 轮次卡 / 工具回执 / 收场语里摞新句子。

- **判据**：diff 让任何送模文案变长了 → 违律。
- **替代**：先问「删掉哪句能解决」；删无可删，就把信息交给模型自己判断——给通道，不给指令。
- **案例**：8/02 逼一张没有状态栏的卡交状态栏，模型当场发明了一个标签塞进正文；8/10「每拍在正文之后按同样的块结构输出」一句话，让模型把状态栏写进了稿件。

## 二、禁止堆叠注入

不新增注入点、注入时机、注入种类。注入通道是封闭集合，每个通道的内容有唯一主人；一个事故想靠「在 X 时机再塞一段内容给模型」解决 → 停手，那就是堆叠。

- **判据**：修复方案的形状是「新增一处向模型塞话的地方」→ 违律。
- **替代**：在既有通道内改写（同时遵守铁律一），或改走数据/工具通道。

## 三、禁止堆叠架构（识别器 / 名单 / 特判）

不新增枚举「别人发明的名字」的正则、名单、启发式分支；现存名单只许冻结、收缩、删除，不许添行。harness 只认自己发行的协议（工具调用、模型声明的合约），不认卡作者/预设作者的措辞。

- **判据**：diff 往 FOLD_NAME_RE / PANEL_NAME_RE / STRIP_NAME_RE / TAIL_INTENT_RE / cardStatusBarFormats / HISTORY_STRIP_BUILTIN 这类名单加了条目，或新建了同类物 → 违律。
- **替代**：让读得懂卡的模型声明一次，声明落成数据（合约/台本/清单），harness 死板执行数据——错了也错在一份看得见、改得动的数据上，不是错在一段看不见的正则行为里。
- **案例**：状态栏名单在 cardfront / postprocess / web statusBlocks 三处平行、互相追赶；用户预设里角色的昵称被硬编码进引擎源码当意图关键词。

## 四、时刻整体性思考

动手前必须答出三问，答不出就不动代码——记录，等机制：

1. 这个问题的**全集**是什么？（不是「这张卡这个症状」——换一张没见过的卡，它长什么样？）
2. 现有机制里**谁该对它负责**？（找不到负责人＝缺机制，不是缺补丁。）
3. 这个改动在**没见过的卡**上会怎么表现？（答不出＝潜在污染。）

## 执行

- 修复方案违律即默认否决。**已记录的缺陷等机制解，好过多一层堆叠**（8/10 用户定案）。
- 用户点名修什么就修什么；症状怎么归类以用户为准，不自行升格扩权。
